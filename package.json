{
  "name": "dogapi-endpoint-test-automation",
  "version": "1.0.0",
  "description": "Testes automatizados para validar endpoints da Dog API usando Cypress",
  "scripts": {
    "cypress:open": "cypress open",
    "cypress:run": "cypress run",
    "report:merge": "npx mochawesome-merge cypress/results/*.json > cypress/results/report.json",
    "report:generate": "npx mochawesome-report-generator cypress/results/report.json",
    "report": "npm run report:merge && npm run report:generate",
    "test": "npm run cypress:run && npm run report"
  },
  "devDependencies": {
    "cypress": "^13.0.0",
    "mochawesome": "^7.1.3",
    "mochawesome-merge": "^4.2.0",
    "mochawesome-report-generator": "^6.0.1"
  }
}
